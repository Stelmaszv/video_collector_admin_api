{% extends 'base.html.twig' %}
{% block title %} Series {% endblock %}
{% block body %}
{{ include('@navbars/collectornavbar.twig') }}
<ul>
    <table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
        </tr>
    </thead>
    <tbody>
        {% for el in object %}
        <tr>
            <th scope="row">{{el.id}}</th>
            <td><a href={{path('StarDetail',{id:el.id,collector:Collector})}}>{{el.name()}}</a></td>
        </tr>
        {% endfor %}
    </tbody>
    </table>
<ul>
{{ knp_pagination_render(object) }}
{% endblock %}